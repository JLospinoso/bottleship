machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t jaarky/bottleship:latest .

test:
  override:
    - docker run jaarky/bottleship:latest /bottleship/bin/tst
